Place local model weights referenced in `data/models.yaml` here if you are not using Ollama.

- Large language model checkpoint (e.g., llama3.1:8b-instruct-q4_0 converted for llama.cpp).
- Embedding model files (if running sentence-transformers offline).

Document any experimental changes, quantization settings, or provenance when updating these assets.
